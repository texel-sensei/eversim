#pragma once

/*
 * Enums for every possible action in every context
 */

namespace eversim {	namespace core { namespace system {

	enum InputConstants
	{
		CONFIRM = 0,
		ABORT,
		JUMP,
		DUCK
	};

}}}